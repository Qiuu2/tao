# 关于 ok112/：那是上一代 PHP 系统，**刻意保留在仓库里**作为业务规则的
# 对照基准 —— 新版后端多处注释直接引用它的文件名说明逻辑出处
# （见 server/internal/terminal/caps.go、keyspec.go 等）。
# 它只作参考，不参与构建也不运行，所以这里不忽略它。
#
# ⚠ 但 ok112/inc/config.php 里有明文数据库口令，ok112/mysql/ 与 ok112/var/
#   是带真实业务数据的 MySQL 数据目录。仓库务必保持私有。

# ── 依赖与构建产物 ────────────────────────────────────────────────────
node_modules/
web/dist/
*.log

# ── 运行时配置（含口令，绝不入库）────────────────────────────────────
# 部署时从 deploy/config.yaml.example 拷一份再改，样例文件本身是入库的。
config.yaml
deploy/config.yaml

# ── 数据库数据目录与备份 ──────────────────────────────────────────────
# 防止今后再把数据库数据文件提交进来。
# ok112/ 下的那份是历史遗留、已入库的对照材料，明确排除在本规则之外，
# 免得规则与「刻意保留 ok112」的意图自相矛盾。
*.MYD
*.MYI
*.frm
ibdata1
ib_logfile*
!ok112/**

# ── 编辑器与系统 ──────────────────────────────────────────────────────
.DS_Store
Thumbs.db
*.swp
*.swn
